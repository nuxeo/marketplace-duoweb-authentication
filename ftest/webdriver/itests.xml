<project name="nuxeo-duoweb-authentication-tests" xmlns:artifact="urn:nuxeo-artifact">
  <taskdef resource="org/nuxeo/build/antlib.xml" uri="urn:nuxeo-build" />
  <taskdef resource="org/nuxeo/build/artifact/antlib.xml" uri="urn:nuxeo-artifact" />
  <taskdef resource="net/sf/antcontrib/antlib.xml" />

  <property name="out.dir" value="${maven.project.build.directory}" />
  <property name="mp.install"
            value="file:${out.dir}/nuxeo-marketplace-jsf-ui-${nuxeo.distribution.version}.zip,
    file:${out.dir}/marketplace-duoweb-authentication-${marketplace.duoweb.authentication.version}.zip" />
  <unzip dest="${out.dir}/" overwrite="false">
    <artifact:resolveFile key="org.nuxeo:nuxeo-ftest::zip" />
  </unzip>
  <import file="${out.dir}/nuxeo-ftest.xml" />

  <target name="prepare-environment" depends="_init,prepare-db,prepare-tomcat">
    <copy todir="${out.dir}">
      <artifact:file key="org.nuxeo.ecm.distribution:nuxeo-marketplace-jsf-ui::zip" />
      <artifact:file key="org.nuxeo.marketplace.duoweb.authentication:marketplace-duoweb-authentication::zip" />
    </copy>
  </target>

</project>
